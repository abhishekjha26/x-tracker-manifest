apiVersion: apps/v1
kind: Deployment 
metadata:
  name: manager-microservice
  labels:
    app: manager-restapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: manager-restapp
  template:  
    metadata:
      labels: 
        app: manager-restapp
    spec:
      # initContainers:
      #   - name: init-db
      #     image: busybox:1.31
      #     command: ['sh', '-c', 'echo -e "Checking for the availability of MySQL Server deployment"; while ! nc -z mysql 3306; do sleep 1; printf "-"; done; echo -e "  >> MySQL DB Server has started";']      
      containers:
        - name: manager
          image: papeterinnova/manager-api:v0.0.1-release
          # command: [ "sleep" ]
          # args: [ "infinity" ]
          ports: 
            - containerPort: 8080
          livenessProbe:
            # exec:
            #   command: 
            #     - /bin/sh
            #     - -c 
            #     - nc -z localhost 8080
            httpGet:
              path: /managers/test
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /managers/test
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10